{% block element_vo_video %}
<div class="vo-cms-el-vo-video" style="display:flex;align-items:center;justify-content:center;min-height:120px;background:#f0f2f5;border-radius:4px">
    <template v-if="videoUuid">
        <mt-icon name="regular-play-circle" size="32" />
        <span style="margin-left:8px">{{ videoUuid }}</span>
    </template>
    <span v-else>{{ $tc('vo-media.cms.selectVideo') }}</span>
</div>
{% endblock %}
